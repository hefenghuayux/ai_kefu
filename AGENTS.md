# AGENTS.md

## 通用规则

1. 当任务可以并行运行时，你可以开启一个或者多个子代理完成任务。
2. 不要写兜底函数或者其他冗余的封装，只需要留下报错信息的打印。
3. 在必要的地方写上注释，不要删去原有代码的注释。
4. 不要轻易删去或者修改原有的功能，这可能是我做的优化设计；在你进行改动前，需要先告知我。
5. 你的回复一律使用中文，即使我输入的请求是英文。
6. 如果你有不确定的问题就不要行动，先询问我的意见。
7. 当你提出解决方案或观点时，最后必须补充该方案/观点的合理性批判或不足分析。当我提出解决方案或观点时，也要补充其合理性批判或不足分析。
8. 当一个问题反复提问但仍未被解决时，要质疑当前解决方向是否有问题，并提出其他可能方向。

## 代码解释规则

当我要求“解释代码”时，请使用中文，并严格按照以下格式输出。解释目标不是逐行翻译代码，而是帮助我理解这段代码的系统作用、执行流程、核心机制、设计取舍和潜在问题。

默认解释代码时，思考和查资料时间要克制，只阅读当前文件以及理解它所必需的少量直接依赖。不要花大量 token 主动搜索完整调用链、跨模块关系、历史实现或大范围项目结构；只有当我明确要求“结合调用链”“深入分析整个模块关系”“从入口追踪到实现”“系统性讲解项目实现”等类似任务时，才进行较大范围搜索。若当前文件单独解释会存在明显误判风险，可以简短说明需要补充看的 1-2 个文件，再进行最小范围确认。

1. 简单解释函数/类/模块的作用

用几段简洁文字说明代码整体作用。如果包含多个函数、类或方法，需要分别说明它们的职责，以及它们在调用链、数据流、控制流或系统分层中的位置。

不要只说“代码做了什么”，还要说明“它为什么存在”“它解决了什么问题”。

2. 以代码注释形式分析代码

这一部分必须输出一个完整代码块，保留原始代码结构。不要拆成多个代码片段，不要在代码块外逐行解释，不要用表格替代代码块。

注释要求：

- 不需要强行给每一行都加注释。
- import 要注释，帮助我理解系统依赖的导入
- 函数、方法和类构造器的签名也要注释，解释关键参数、默认值、返回类型和回调参数的含义；对于异步函数、流式接口、生成器、回调函数等，要说明参数如何参与执行流程。
- 普通赋值、明显 return 等代码可以少注释或不注释。
- 对影响理解的语言特性、框架用法和语法机制要主动注释，例如 `@staticmethod`、装饰器、异步函数、异步生成器、上下文管理器、类型注解、枚举比较、依赖注入、回调函数、流式响应等。注释不只说明“是什么”，还要说明“为什么这里适合这样用”。
- 重点注释关键变量、核心函数、重要分支、状态变化、数据结构、配置项、算法步骤、张量形状、内存布局、并发/异步/缓存/调度逻辑等。
- 已有注释必须保留，不要删除；可以在已有注释旁补充更准确的解释。
- 注释应说明“这段代码在流程中的作用”和“为什么这样写”，不要只机械翻译代码。

3. 分析代码中的核心要点，给出补充理解

这一部分必须使用“小标题：正文”的结构。每个小标题后面用一段简洁正文解释。

核心要点最多写 3 点，只选择最重要、最能帮助理解当前代码的内容。不要为了完整性罗列所有细节。

每个核心要点都必须包含两层内容：

- 当前代码体现了什么机制、流程、设计思想或专业术语。
- 这个机制为什么重要，或者它在系统中解决了什么问题。

如果可以用准确术语表达，不要只用泛泛描述。例如优先写“缺乏单例控制”，而不是只写“每次都会重新创建对象”；优先写“简单工厂模式”，而不是只写“集中创建对象”。但术语不能空喊，必须结合当前代码解释其适用边界。

可以从设计模式、调用链、数据流、状态流转、算法原理、张量计算、量化机制、内存管理、调度策略、性能影响、工程可维护性等角度选择，但必须结合当前代码，不要空泛堆术语。

合理性批判/不足分析：

这一部分最多写 3 点，只写最重要的设计问题或潜在风险。

批判要优先从设计、原理、系统约束和工程演进角度分析，也可以说明在哪些场景下会变得不够好。能使用准确术语时应使用术语，例如“违反开闭原则”“缺乏单例控制”“配置校验不显式”“耦合到具体实现”“异常吞掉导致可观测性不足”等，并解释为什么这个术语适用于当前代码。不要把重点放在无关紧要的代码洁癖上。类似未使用 import 这种问题，只有在确实影响维护判断时才简略提到。
